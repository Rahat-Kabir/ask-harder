I'd use a token bucket per API key. Each key gets a bucket that refills at a fixed rate, and every request consumes a token; when the bucket is empty you return 429. Token bucket is nice because it allows short bursts while still capping the average rate. I'd store the buckets in Redis so the counts are shared, with the key being the API key and a Lua script to make the check-and-decrement atomic.
