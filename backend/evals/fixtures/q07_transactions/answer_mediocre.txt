When two transactions run at the same time, one can see data the other is in the middle of changing, which causes weird results like reading a value that later gets rolled back. Isolation levels control how much of that is allowed. The levels are read uncommitted, read committed, repeatable read, and serializable, from weakest to strongest. Most databases default to read committed. Higher levels are safer but slower.
