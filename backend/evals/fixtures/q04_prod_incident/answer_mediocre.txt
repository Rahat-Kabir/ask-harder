We had an incident where our checkout API started timing out during an evening peak. I got paged, looked at the logs, and saw a lot of slow database queries. We found that a recent deploy had added a query without an index. We rolled back the deploy and the timeouts stopped, and the next day the author added the index and re-deployed. Customers couldn't complete purchases for around forty minutes.
