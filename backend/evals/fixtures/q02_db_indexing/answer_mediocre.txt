An index is like the index in a book — instead of reading every page, the database can jump straight to the rows it needs, so it avoids scanning the whole table. The downside is that indexes take up extra disk space, and there is some overhead to keep them updated. So you shouldn't index every column, just the ones you query by often.
