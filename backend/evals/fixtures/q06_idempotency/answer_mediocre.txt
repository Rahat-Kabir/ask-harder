The danger is that the first request actually succeeded and the response just got lost, so the retry creates a duplicate — like charging a card twice. The usual fix is an idempotency key: the client generates a unique ID and sends it in a header, and the server remembers which keys it has already processed. If it sees the same key again, it doesn't redo the work. Stripe's API works this way.
